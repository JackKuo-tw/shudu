---
phase: 02-browser-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, playwright.config.ts, tests/pom/ShuduPage.ts, tests/pom/WasmBridge.ts, tests/smoke.test.ts]
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Playwright is installed and configured to run against a local server"
    - "Page Object Model (POM) abstracts editor interactions"
    - "WasmBridge ensures tests wait for WASM initialization"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Test configuration and webServer setup"
    - path: "tests/pom/ShuduPage.ts"
      provides: "Editor interaction abstraction"
    - path: "tests/pom/WasmBridge.ts"
      provides: "WASM synchronization logic"
  key_links:
    - from: "tests/pom/ShuduPage.ts"
      to: ".ql-editor"
      via: "Playwright locator"
    - from: "tests/pom/WasmBridge.ts"
      to: "window.shudu.ready"
      via: "page.evaluate/waitForFunction"
---

<objective>
Setup Playwright foundation, configuration (including webServer), and the base Page Object Model (POM) with WASM synchronization.

Purpose: To provide a stable and maintainable testing infrastructure for Shudu's WASM-based editor.
Output: Playwright configuration, POM classes, and a basic smoke test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@web/index.html
@web/public/javascripts/query.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Playwright and Dependencies</name>
  <files>package.json</files>
  <action>
    Install necessary dependencies for E2E testing:
    - `npm install --save-dev @playwright/test typescript http-server`
    - Initialize Playwright if needed (or just install browsers): `npx playwright install --with-deps chromium`
    
    Update `package.json` to include a `test:e2e` script: `"test:e2e": "playwright test"`.
  </action>
  <verify>
    <automated>npm list @playwright/test http-server && grep "test:e2e" package.json</automated>
  </verify>
  <done>Dependencies installed and test script added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright with Web Server</name>
  <files>playwright.config.ts</files>
  <action>
    Create `playwright.config.ts` with the following requirements (per D-05 and TEST-01):
    - Set `testDir` to `./tests`.
    - Configure `webServer` to run `npx http-server web/ -p 8080` (ensure it waits for the port).
    - Set `use.baseURL` to `http://localhost:8080`.
    - Set `use.browserName` to `chromium` (standard for WASM testing).
    - Set a reasonable `timeout` (e.g., 30s) and `actionTimeout` (15s) to account for WASM compilation on CI.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && grep "webServer" playwright.config.ts</automated>
  </verify>
  <done>Playwright configuration created with local server support.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Page Object Model (POM)</name>
  <files>tests/pom/ShuduPage.ts, tests/pom/WasmBridge.ts</files>
  <action>
    Implement POM classes (per TEST-02 and Research):
    
    1. **WasmBridge.ts**:
       - Method `waitForReady()`: Uses `page.waitForFunction(() => window.shudu && window.shudu.ready)` or similar to ensure WASM is loaded.
    
    2. **ShuduPage.ts**:
       - Constructor accepts Playwright `Page`.
       - `goto()`: Navigates to `/`.
       - `fillEditor(text: string)`: Targets `.ql-editor` inside `#origin` and uses `locator.fill()`.
       - `getEditorContent()`: Returns `innerText()` of `.ql-editor`.
       - `selectTranslation(value: 's2twp' | 'tw2sp')`: Selects value in `#translation`.
       - `selectPunctuation(value: 'fullWidth' | 'default')`: Selects value in `#punctuation`.
       - `clickConvert()`: Clicks `#convert button[type="submit"]`.
       - Includes an instance of `WasmBridge`.
  </action>
  <verify>
    <automated>test -f tests/pom/ShuduPage.ts && test -f tests/pom/WasmBridge.ts</automated>
  </verify>
  <done>POM classes implemented with editor and WASM abstractions.</done>
</task>

<task type="auto">
  <name>Task 4: Basic Smoke Test</name>
  <files>tests/smoke.test.ts</files>
  <action>
    Create a basic smoke test `tests/smoke.test.ts` that:
    1. Initializes `ShuduPage`.
    2. Navigates to the app.
    3. Waits for WASM readiness using the POM.
    4. Verifies the page title or a key element (e.g., header "Shudu 舒讀") is visible.
  </action>
  <verify>
    <automated>npx playwright test tests/smoke.test.ts</automated>
  </verify>
  <done>Smoke test passes, confirming infrastructure is working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Local Server | Test runner interacts with served files via HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | Local Server | mitigate | Ensure `http-server` only serves the `web/` directory, not the whole project. |
| T-02-01-02 | Denial of Service | Playwright Config | accept | Port collision handled by Playwright's `reuseExistingServer`. |
</threat_model>

<verification>
Run `npx playwright test tests/smoke.test.ts` to verify the foundation.
</verification>

<success_criteria>
- Playwright is configured with a local server.
- POM classes are implemented and functional.
- Smoke test passes in headless mode.
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-automation/02-01-SUMMARY.md`
</output>
