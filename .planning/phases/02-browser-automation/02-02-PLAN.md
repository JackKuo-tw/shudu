---
phase: 02-browser-automation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [tests/conversion.test.ts, tests/pangu.test.ts, .github/workflows/main.yml]
autonomous: true
requirements: [TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "Simplified to Traditional (Taiwan) conversion is verified"
    - "Traditional to Simplified conversion is verified"
    - "Pangu spacing between CJK and Latin/Numbers is verified"
    - "Tests run automatically in GitHub Actions"
  artifacts:
    - path: "tests/conversion.test.ts"
      provides: "Verification of OpenCC conversion logic"
    - path: "tests/pangu.test.ts"
      provides: "Verification of typography spacing logic"
    - path: ".github/workflows/main.yml"
      provides: "CI automation for Playwright tests"
  key_links:
    - from: "tests/conversion.test.ts"
      to: "ShuduPage"
      via: "Method calls"
    - from: ".github/workflows/main.yml"
      to: "npx playwright test"
      via: "Action step"
---

<objective>
Implement specific test suites for conversion and spacing, and integrate E2E tests into the GitHub Actions CI workflow.

Purpose: To ensure the core functionality of Shudu remains intact during future development and to catch regressions automatically.
Output: Conversion and spacing test suites, updated CI workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-automation/02-01-PLAN.md
@tests/pom/ShuduPage.ts
@tests/pom/WasmBridge.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Conversion Flow Tests</name>
  <files>tests/conversion.test.ts</files>
  <action>
    Implement E2E tests for conversion paths (per TEST-03):
    1. **S2T (Taiwan) Test**:
       - Input: "鼠标在手,光标跟我走!"
       - Select: "繁體台灣用語"
       - Result: "滑鼠在手，游標跟我走！"
    2. **T2S Test**:
       - Input: "燕燕于飛，差池其羽。"
       - Select: "簡體中國用語"
       - Result: "燕燕于飞，差池其羽。"
    3. **Editor Persistence**:
       - Verify that typing into the editor, then converting, preserves the content structure (no unexpected data loss).
  </action>
  <verify>
    <automated>npx playwright test tests/conversion.test.ts</automated>
  </verify>
  <done>Conversion tests pass, verifying S2T, T2S, and basic persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Pangu Spacing Tests</name>
  <files>tests/pangu.test.ts</files>
  <action>
    Implement tests for Pangu spacing (per TEST-04):
    1. **CJK and Latin**:
       - Input: "A中"
       - Punctuation: "舒適使用全形"
       - Result: "A 中"
    2. **CJK and Numbers**:
       - Input: "1中"
       - Punctuation: "舒適使用全形"
       - Result: "1 中"
    3. **Consistency**:
       - Verify that multiple conversions do not result in double-spacing or other artifacts.
  </action>
  <verify>
    <automated>npx playwright test tests/pangu.test.ts</automated>
  </verify>
  <done>Pangu spacing tests pass, verifying typography rules.</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions Integration</name>
  <files>.github/workflows/main.yml</files>
  <action>
    Add a new job `playwright-test` to `.github/workflows/main.yml` (per TEST-05):
    - Run on `ubuntu-latest`.
    - Steps:
      1. Checkout code.
      2. Setup Node.js.
      3. Install dependencies (`npm ci`).
      4. Install Playwright browsers (`npx playwright install --with-deps chromium`).
      5. Run tests (`npx playwright test`).
      6. Upload Playwright report on failure (`actions/upload-artifact@v4`).
    
    Ensure this job runs on push to `master` and on pull requests.
  </action>
  <verify>
    <automated>grep "playwright-test" .github/workflows/main.yml</automated>
  </verify>
  <done>GitHub Actions updated with automated Playwright testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → GitHub | Test results and reports uploaded to GitHub infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | CI Workflow | mitigate | Use pinned versions for actions (e.g., `v4`) and ensure only authorized pushes to master. |
| T-02-02-02 | Resource Exhaustion | CI Runs | accept | WASM tests can be heavy; Playwright uses chromium-only to save time. |
</threat_model>

<verification>
Run `npx playwright test` locally to ensure all tests pass before committing CI changes.
</verification>

<success_criteria>
- Conversion tests pass.
- Pangu tests pass.
- GitHub Actions workflow is valid and contains the Playwright job.
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-automation/02-02-SUMMARY.md`
</output>
