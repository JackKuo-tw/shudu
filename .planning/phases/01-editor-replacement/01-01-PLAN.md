---
phase: 01-editor-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/index.html, web/public/stylesheets/style.css, web/public/javascripts/query.js, web/public/javascripts/zh_TW.js]
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-05]
must_haves:
  truths:
    - "TinyMCE scripts and styles are removed from the page"
    - "Quill v2.0.2 is loaded via CDN"
    - "The editor initializes correctly and allows text entry"
    - "Conversion logic works with the new Quill instance"
    - "Editor styling matches the Shudu UI (12px rounded corners)"
  artifacts:
    - path: "web/index.html"
      provides: "Quill assets and container"
    - path: "web/public/stylesheets/style.css"
      provides: "Quill UI overrides"
    - path: "web/public/javascripts/query.js"
      provides: "Quill initialization and API integration"

---

<objective>
Replace TinyMCE 4 with Quill v2 in the web interface to modernize the rich-text editing experience and improve performance.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/UI-SPEC.md
@web/index.html
@web/public/stylesheets/style.css
@web/public/javascripts/query.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Assets and HTML Structure</name>
  <files>web/index.html, web/public/javascripts/zh_TW.js</files>
  <action>
    - In `web/index.html`:
      - Remove TinyMCE script tag.
      - Add Quill CSS CDN in head: `https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.snow.css`.
      - Add Quill JS CDN before other scripts: `https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.js`.
      - Replace `<textarea id="origin" ...></textarea>` with `<div id="origin" style="height: 300px;"></div>`.
    - In `web/public/javascripts/zh_TW.js`:
      - Clear the file content as it contains TinyMCE-specific translations that are no longer needed (per WEB-01).
  </action>
  <verify>
    grep "quill" web/index.html && ! grep "tinymce" web/index.html
  </verify>
  <done>Assets updated and TinyMCE references removed from HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Quill Styling Overrides</name>
  <files>web/public/stylesheets/style.css</files>
  <action>
    - Remove TinyMCE-specific overrides (lines 207-212).
    - Add Quill overrides per UI-SPEC.md:
      - Set `.ql-toolbar.ql-snow` border-radius to `12px 12px 0 0`.
      - Set `.ql-container.ql-snow` border-radius to `0 0 12px 12px`.
      - Ensure both have `1px solid var(--border-color)`.
      - Set `.ql-editor` min-height to 300px and font-family to 'Inter'.
  </action>
  <verify>
    grep ".ql-toolbar" web/public/stylesheets/style.css
  </verify>
  <done>Quill editor styled to match application design.</done>
</task>

<task type="auto">
  <name>Task 3: Re-implement Editor Logic in query.js</name>
  <files>web/public/javascripts/query.js</files>
  <action>
    - Define a global `quill` variable.
    - Replace `tinymce.init` with `quill = new Quill('#origin', { theme: 'snow', modules: { toolbar: [...] } })`.
    - Update `getContent()`:
      - Use `quill.getSemanticHTML()` (or `quill.root.innerHTML`) as the source for `parseHTML`.
    - Update `setContent()`:
      - Use `quill.root.innerHTML = virtualBody.innerHTML` (or `quill.clipboard.dangerouslyPasteHTML`) to update the content.
    - Replace `tinyMCE.activeEditor.setProgressState(true/false)` with:
      - `quill.enable(false/true)`
      - Adding/removing a loading state to the submit button (e.g., changing text or adding a class).
  </action>
  <verify>
    ! grep "tinymce" web/public/javascripts/query.js
  </verify>
  <done>Application logic successfully migrated to Quill API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual and Functional Verification</name>
  <what-built>A fully functional Quill editor replacing TinyMCE.</what-built>
  <how-to-verify>
    1. Open `web/index.html` in a browser.
    2. Verify the editor appears with rounded corners and the correct toolbar.
    3. Enter some text (e.g., "鼠标在手").
    4. Click "立即轉換".
    5. Verify the text is converted to "滑鼠在手" and formatting is preserved.
    6. Try some formatting (Bold, H1) and verify it persists after conversion.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser Context | Quill operates entirely in the client-side DOM. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | query.js | mitigate | Use `dangerouslyPasteHTML` only with trusted conversion output from local shudu core. |
| T-01-02 | Information Disclosure | editor content | accept | Processing is 100% client-side as per privacy banner. |
</threat_model>

<success_criteria>
- TinyMCE is completely removed from the project.
- Quill is integrated and styled correctly.
- All conversion features work as expected.
- No regression in UI/UX.
</success_criteria>

<output>
After completion, create `.planning/phases/01-editor-replacement/01-01-SUMMARY.md`
</output>
